SET(SRC_LIST test2.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})

LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUG_DEFS})
ADD_EXECUTABLE(test2 ${SRC_LIST})
TARGET_LINK_LIBRARIES(test2 ${TOOLS_LINK_LIBS})


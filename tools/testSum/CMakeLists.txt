SET(SRC_LIST PSum.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})

LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUG_DEFS})
ADD_EXECUTABLE(testSum ${SRC_LIST})
TARGET_LINK_LIBRARIES(testSum ${TOOLS_LINK_LIBS})


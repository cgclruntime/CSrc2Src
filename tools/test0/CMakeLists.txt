SET(SRC_LIST test0.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})

LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUG_DEFS})
ADD_EXECUTABLE(test0 ${SRC_LIST})
TARGET_LINK_LIBRARIES(test0 ${TOOLS_LINK_LIBS})


SET(SRC_LIST test1.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})

LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUG_DEFS})
ADD_EXECUTABLE(test1 ${SRC_LIST})
TARGET_LINK_LIBRARIES(test1 ${TOOLS_LINK_LIBS})

